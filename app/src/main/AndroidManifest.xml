<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.vs.sheriff">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <activity
            android:name=".ui.activity.Main"
            android:label="Sheriff" />

        <activity
            android:name=".ui.activity.ListStock"
            android:label="Estoque" />

        <activity
            android:name=".ui.activity.NewStock"
            android:label="Estoque" />

        <activity
            android:name=".ui.activity.NewLogin"
            android:label="Criar conta" />

        <activity
            android:name=".ui.activity.Login"
            android:label="Sheriff">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.activity.ListProduct"
            android:label="Produto" />

        <activity
            android:name=".ui.activity.NewProduct"
            android:label="Produto" />

        <activity
            android:name=".ui.activity.UserProfile"
            android:label="Perfil" />

    </application>

</manifest>